[user]
	name = tomdewildt
	email = 32359630+tomdewildt@users.noreply.github.com
[push]
    default = simple
[alias]
    co = checkout
    ci = commit
    st = status
    fe = fetch
    br = branch
    pl = pull
    df = diff
    hist = log --pretty=format:\"%h %ad | %s%d [%an]\" --graph --date=short
    type = cat-file -t
    dump = cat-file -p
    ls = log --oneline -n 20 --graph
[credential]
	helper = store
